# Pinned runtime dependencies for attention-blackwell.
#
# Install from inside the activated `gpu` venv:
#
#     pip install -r env/requirements.txt
#
# torch is installed from the PyTorch CUDA 12.8 wheel index. The local-version
# specifier (+cu128) matches the existing `gpu` venv install exactly so this is
# idempotent.

--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.9.1+cu128

# Numerics + IO
numpy==2.1.3
pyyaml==6.0.2
pyarrow==18.1.0
pandas==2.2.3

# Plotting
matplotlib==3.9.3

# CLI / output
rich==13.9.4

# Build tools (required by torch.utils.cpp_extension for both JIT and AOT CUDA builds)
ninja==1.11.1.4

# Testing
pytest==8.3.4
